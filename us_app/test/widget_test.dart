// Smoke test placeholder — full tests will be added per feature.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // TODO: replace with real widget tests as screens are built out.
    expect(true, isTrue);
  });
}
